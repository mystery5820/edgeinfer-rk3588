server:
  host: 0.0.0.0
  port: 8080
  log_level: info

runtime:
  default_backend: rkllm
  enable_vision: true
  enable_llm: true
  enable_vlm: false

paths:
  model_root: ./models
  result_root: ./results
  log_root: ./results/logs
